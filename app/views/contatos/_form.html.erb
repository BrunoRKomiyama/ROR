<%= form_with(model: contato,:html => {:class => 'formulario'}) do |form| %>
  <h2>Novo Contato</h2>
  <% if contato.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(contato.errors.count, "error") %> prohibited this contato from being saved:</h2>

      <ul>
        <% contato.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-itens">
    <div>
      <%= form.label :nome, style: "display: block" %>
      <%= form.text_field :nome %>
    </div>

    <div>
      <%= form.label :telefone, style: "display: block" %>
      <%= form.text_field :telefone %>
    </div>

    <div>
      <%= form.submit 'Enviar', :class => 'btn azul' %>
    </div>
  </div>
<% end %>
